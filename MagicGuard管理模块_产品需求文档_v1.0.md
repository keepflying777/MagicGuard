# 数据安全模块（DSM）产品需求文档（PRD）

| 文档信息 | |
|---|---|
| 文档版本 | v1.0 |
| 文档状态 | 待评审 |
| 编写日期 | 2026-07-20 |
| 编写人 | （项目甲方） |
| 评审人 | （待定：技术负责人、安全负责人、业务方代表） |
| 保密级别 | 商密二级 |

---

## 0. 文档说明

本文档基于以下两份原始输入整理：
- 《数安管理模块响应技术参数》（采购方原始需求）
- 与产品/技术顾问的多轮沟通记录

**文档定位**：作为项目立项、产品设计、外包招标、POC 验收的统一参考文档。后续将根据评审意见持续迭代。

**重要约束**：
- 性能硬指标"在线洗数 100 万/4min、反洗数 100 万/4min"经项目方确认**不作为本次硬性要求**，但**脱敏任务应在合理时间内完成**（参考：100 万条 ≤ 30 分钟）。
- 第三方检测报告**不作为本次强制要求**，但所有功能需通过内部测试用例验收。

---

## 1. 项目背景

### 1.1 业务背景

随着《数据安全法》《个人信息保护法》《密码法》等法规陆续实施，以及等保 2.0、密评等合规要求不断加码，组织面临的数据安全合规压力持续增大。数据资产作为核心生产要素，需要在"可管、可控、可用、可溯"四个维度上构建完整保护体系。

### 1.2 痛点

| # | 痛点 | 描述 |
|---|---|---|
| 1 | 敏感数据"裸奔" | 业务数据库中的身份证、手机号、银行卡、薪资等敏感字段以明文存储，存在泄露风险 |
| 2 | 内外数据流转失控 | 生产数据导出到开发、测试、外发场景时，缺乏统一脱敏管控 |
| 3 | 身份认证薄弱 | 管理员账号仅靠密码登录，缺乏强身份认证手段 |
| 4 | 应用改造成本高 | 传统加密方案要求业务系统改造代码，落地周期长、改动大 |
| 5 | 合规举证难 | 数据访问、操作、修改缺乏完整审计链路，无法满足监管检查 |

### 1.3 项目目标

构建一套**数据安全模块（DSM）**，对外提供数据脱敏与透明加密能力，覆盖数据"存储、传输、使用、外发"全生命周期，达成：

- **业务目标**：保护敏感数据，支撑业务安全合规运转
- **技术目标**：业务系统零改造，透明接入
- **合规目标**：满足等保 2.0、商用密码应用安全性评估（密评）要求

---

## 2. 术语与缩略语

| 术语 | 解释 |
|---|---|
| **DSM** | Data Security Module，数据安全模块（本项目） |
| **DSM-MASK** | 数据脱敏子系统 |
| **DSM-ENC** | 加密网关子系统 |
| **DSM-Console** | 统一管理控制台 |
| **KMS** | Key Management System，密钥管理系统 |
| **HSM** | Hardware Security Module，硬件安全模块 |
| **DEK** | Data Encryption Key，数据加密密钥 |
| **MK** | Master Key，主密钥 |
| **FPE** | Format Preserving Encryption，保形加密 |
| **UKEY** | USB Key，智能密码钥匙 |
| **CA** | Certificate Authority，证书颁发机构 |
| **CRL** | Certificate Revocation List，证书吊销列表 |
| **OCSP** | Online Certificate Status Protocol，在线证书状态协议 |
| **密评** | 商用密码应用安全性评估 |

---

## 3. 产品架构

### 3.1 整体架构

```
┌──────────────────────────────────────────────────────────────┐
│                       业务应用层（零改造）                    │
│   CRM系统  │  OA系统   │  HR系统   │  BI分析   │  其他业务   │
└────────────┬─────────────┬──────────┬─────────┬─────────────┘
             │             │          │         │
             ▼             ▼          ▼         ▼
┌──────────────────────────────────────────────────────────────┐
│              DSM-Console  管理控制台（Web）                  │
│   密钥管理 │  规则配置  │  任务调度  │  审计日志  │  系统管理  │
└────────────┬─────────────────────────────────────────────────┘
             │
   ┌─────────┴──────────┐
   ▼                    ▼
┌──────────────┐   ┌─────────────────┐
│ DSM-ENC      │   │   DSM-MASK      │
│ 加密网关子系统 │   │   数据脱敏子系统  │
│ (代理模式)    │   │   (任务调度)     │
├──────────────┤   ├─────────────────┤
│ · UKEY 认证  │   │ · 静态脱敏任务  │
│ · 协议解析   │   │ · 动态脱敏规则  │
│ · 透明加解密 │   │ · 在线洗数      │
│ · 完整性校验 │   │ · 反洗数        │
│ · 密文检索   │   │ · 数据导出      │
└──────┬───────┘   └────────┬────────┘
       │                    │
       ▼                    ▼
┌──────────────┐   ┌─────────────────┐
│  业务数据库   │   │  业务数据库（多） │
│ (零改造)     │   │  源库 → 目标库    │
└──────────────┘   └─────────────────┘
       │
       ▼
┌──────────────────────────────────────────┐
│  KMS + HSM（密钥管理）                    │
│  · SM2/SM3/SM4 国密算法                   │
│  · 密钥生成/轮换/销毁                      │
│  · 凭据管理                                │
└──────────────────────────────────────────┘
```

### 3.2 技术选型建议

| 组件 | 建议方案 | 备选 |
|---|---|---|
| 加密网关底座 | Apache ShardingSphere-Proxy | MyCAT、自研 |
| 国密算法库 | Bouncy Castle（Java） | TONGYI 商用密码 SDK |
| 密钥管理 | HSM 硬件 + 自研 KMS 薄壳 | 商用 KMS |
| 控制台前端 | Vue 3 + Naive UI / Element Plus | React + Antd |
| 控制台后端 | Spring Boot 3.x | 任意 Java 框架 |
| 数据库 | MySQL 8.0 / PostgreSQL 14+ | 达梦、人大金仓（信创） |
| 缓存 | Redis | – |
| 审计日志 | ClickHouse / Elasticsearch | – |

> 详细技术选型理由见附录 A。

---

## 4. 功能需求

本模块对外提供两大子系统：
- **A. 数据脱敏子系统（DSM-MASK）**
- **B. 加密网关子系统（DSM-ENC）**

### A. 数据脱敏子系统（DSM-MASK）

#### A-1. 对称密钥管理

**功能目的**：统一管理业务系统使用到的所有对称密钥，支持密钥生成、存储、轮换、销毁全生命周期。

**详细功能点**：

| 编号 | 功能 | 说明 |
|---|---|---|
| A-1.1 | 密钥生成 | 支持国密 SM4（默认）、AES-128/256；可配置密钥长度、算法、用途标识 |
| A-1.2 | 密钥存储 | 主密钥（MK）由硬件 HSM 保护，**密钥明文不出硬件**；DEK 密文入库 |
| A-1.3 | 密钥分发 | 通过 API 提供加解密接口，**不返回密钥明文** |
| A-1.4 | 密钥轮换 | 支持定时轮换（按天/周/月）+ 手动触发；旧密钥保留用于解密历史数据 |
| A-1.5 | 密钥销毁 | 支持逻辑销毁（标记作废）+ 物理销毁（HSM 域内删除） |
| A-1.6 | BYOK | 支持客户自带密钥导入，在 HSM 内部生成使用 |
| A-1.7 | 备份恢复 | 加密备份 + 多副本；恢复需 2 人以上授权 |

**验收标准**：
- ✅ 密钥明文不在日志、网络报文、API 响应中泄露
- ✅ HSM 断电/重启后，历史数据可正常解密
- ✅ 密钥轮换期间业务系统不中断
- ✅ 单条密钥销毁请求需要二次授权（密码 + 短信/UKEY）

---

#### A-2. 一字段一密钥配置（字段级密钥绑定）

**功能目的**：对敏感字段进行粒度到字段的密钥绑定，每个字段独立密钥，互不关联。

**详细功能点**：

| 编号 | 功能 | 说明 |
|---|---|---|
| A-2.1 | 字段注册 | 在 Console 中登记"数据源.表.字段" |
| A-2.2 | 密钥分配 | 每个字段独立绑定一个 DEK；同一字段在不同表中可用不同密钥 |
| A-2.3 | 密钥矩阵查看 | 可视化展示"字段↔密钥"对应关系，支持导出 |
| A-2.4 | 批量绑定 | 按表/业务系统批量绑定密钥 |
| A-2.5 | 字段标签 | PII / 金融 / 医疗 / 商业秘密 |

**验收标准**：
- ✅ 同一表中 2 个敏感字段必须能用不同密钥独立加解密
- ✅ 删除一个字段的密钥绑定，不影响其他字段
- ✅ 字段密钥矩阵导出文件本身需加密

---

#### A-3. 数据动态脱敏规则管理

**功能目的**：业务系统正常查询时，根据用户身份、查询场景，对敏感字段实时脱敏后返回。

**详细功能点**：

| 编号 | 功能 | 说明 |
|---|---|---|
| A-3.1 | 脱敏算法库 | 内置：掩码、替换、截断、加密、FPE（保形）、Hash、随机化 |
| A-3.2 | 规则配置 | 字段 + 算法 + 参数 + 触发条件；触发条件支持：用户角色、IP 段、应用系统、时间段 |
| A-3.3 | 规则优先级 | 多条规则命中时按优先级排序，第一条生效 |
| A-3.4 | 规则启停 | 支持单条启用/禁用，实时推送 |
| A-3.5 | 规则版本管理 | 每次修改留痕，支持回滚 |
| A-3.6 | 规则测试 | Console 中输入样本数据预演脱敏效果 |

**脱敏算法示例**：

| 字段类型 | 默认算法 | 效果 |
|---|---|---|
| 身份证号 | 掩码 | 110101\*\*\*\*\*\*\*\*1234 |
| 手机号 | 掩码 | 138\*\*\*\*5678 |
| 姓名 | 替换 | 张\* |
| 银行卡 | 截断+掩码 | \*\*\*\* \*\*\*\* \*\*\*\* 1234 |
| 薪资 | 范围化 | 10K-15K |
| 邮箱 | 截断 | a\*\*\*@example.com |

**验收标准**：
- ✅ 同一条数据，不同用户角色看到不同结果
- ✅ 脱敏延迟 < 50ms（不含网络）
- ✅ 规则修改后 5 秒内全网生效

---

#### A-4. 数据静态脱敏规则管理

**功能目的**：对生产数据导出到非生产环境时，进行不可逆脱敏。

**详细功能点**：

| 编号 | 功能 | 说明 |
|---|---|---|
| A-4.1 | 脱敏任务 | 源库 + 目标库/文件 + 表/字段 + 规则集 + 调度 |
| A-4.2 | 多数据源 | MySQL、Oracle、PostgreSQL、SQL Server、达梦、人大金仓、TiDB、Elasticsearch、Hive |
| A-4.3 | 多目标 | 库到库、库到文件、文件到库、文件到文件 |
| A-4.4 | 增量/全量 | 支持全量、增量（按时间字段/主键） |
| A-4.5 | 调度策略 | 立即、定时（Cron）、周期 |
| A-4.6 | 任务审核 | 执行前需审批人确认（**默认开启**） |
| A-4.7 | 失败重试 | 自动重试 3 次 + 告警 |
| A-4.8 | 关联性保留 | 同一身份证在不同表中脱敏后保持一致 |
| A-4.9 | 数据校验 | 脱敏前后行数、字段类型、关联关系校验 |

**验收标准**：
- ✅ 100 万条数据静态脱敏 ≤ 30 分钟（参考值）
- ✅ 脱敏后数据不可逆还原（FPE 模式除外）
- ✅ 任务执行全过程可审计
- ✅ 跨表关联字段脱敏结果一致

---

#### A-5. 业务数据库管理

**功能目的**：统一登记和纳管所有需要保护的数据库。

**详细功能点**：

| 编号 | 功能 | 说明 |
|---|---|---|
| A-5.1 | 数据源注册 | IP、端口、库名、账号（**只读/读写分离**）、协议 |
| A-5.2 | 连接测试 | 添加时实时测试连通性 |
| A-5.3 | 凭据管理 | 数据库密码加密存于 KMS，操作时临时解密 |
| A-5.4 | 数据源探测 | 自动扫描库内表/字段，识别敏感数据（基于规则+正则+AI） |
| A-5.5 | 分组管理 | 按业务系统/环境/部门分组 |
| A-5.6 | 健康监控 | 实时监控连通性、延迟、连接数 |

---

#### A-6. 在线洗数（实时脱敏 + 写回）

**功能目的**：对生产环境正在使用的数据库，在不中断业务的前提下，对存量敏感数据实时脱敏并回写。

**详细功能点**：

| 编号 | 功能 | 说明 |
|---|---|---|
| A-6.1 | 灰度洗数 | 按条件灰度（1% → 10% → 100%） |
| A-6.2 | 增量洗数 | 基于时间字段/变更日志增量处理 |
| A-6.3 | 业务无感 | 洗数过程**无锁表/无阻塞** |
| A-6.4 | 回滚机制 | 洗数前自动备份原值（加密），出问题一键回滚 |
| A-6.5 | 进度可视化 | 实时显示已处理/总数、速率、预计完成时间 |
| A-6.6 | 告警 | 异常率超过阈值（默认 0.1%）自动暂停 + 告警 |

**验收标准**：
- ✅ 100 万条数据在线洗数 ≤ 30 分钟（参考值）
- ✅ 洗数期间业务系统读写延迟增长 < 10%
- ✅ 出错可一键回滚

---

#### A-7. 反洗数（脱敏数据还原）

**功能目的**：在受控场景下（合规审计、司法取证、数据修复），将已脱敏数据还原为明文。

**详细功能点**：

| 编号 | 功能 | 说明 |
|---|---|---|
| A-7.1 | 授权机制 | 至少 2 人授权（业务 + 安全负责人），UKEY 强认证 |
| A-7.2 | 操作审批 | 工单流程，留痕完整 |
| A-7.3 | 范围限制 | 限定还原范围（行数/时间窗/操作人） |
| A-7.4 | 实时水印 | 屏幕叠加可见水印（操作人、时间、原因） |
| A-7.5 | 结果输出 | 还原结果落盘到指定加密目录，不可下载明文 |
| A-7.6 | 全程审计 | 操作录像、键盘记录、数据库 binlog 标记 |
| A-7.7 | 临时凭证 | 一次性凭证，操作完自动销毁 |

**验收标准**：
- ✅ 无授权人员无法触发反洗数（即使拿到数据库权限也不行）
- ✅ 反洗数全过程可追溯到秒

---

### B. 加密网关子系统（DSM-ENC）

#### B-1. UKEY 证书登录

**功能目的**：管理员和审计员登录 DSM-Console 时，必须使用 UKEY 进行双因子认证。

**详细功能点**：

| 编号 | 功能 | 说明 |
|---|---|---|
| B-1.1 | UKEY 类型 | 支持国密 SM2 证书、CFCA/广东 CA 等主流 CA 签发证书 |
| B-1.2 | 驱动兼容 | Windows/macOS/Linux 全平台免驱或自动安装 |
| B-1.3 | PIN 策略 | 6 位以上、支持复杂度、错误 5 次锁死 |
| B-1.4 | 证书校验 | 登录时验证：证书有效期、CRL/OCSP 吊销状态、CA 链 |
| B-1.5 | 双因子 | PIN（知道的）+ UKEY（拥有的）= 必须同时具备 |
| B-1.6 | 证书绑定 | UKEY 与用户账号一对一绑定（首次登录强制绑定） |
| B-1.7 | 离职解绑 | 管理员可远程吊销用户 UKEY 绑定 |
| B-1.8 | 登录日志 | 记录：用户、UKEY 序列号、证书指纹、IP、时间 |

**关键流程**：

```
用户访问 Console → 弹出登录页
   ↓
插入 UKEY → 客户端自动读取证书
   ↓
用户输入 PIN → UKEY 内部用私钥签名一段随机数
   ↓
服务端验证签名 + 证书有效性
   ↓
通过 → 进入 Console（生成会话）
失败 → 锁定 30 分钟（连续失败 5 次）
```

**验收标准**：
- ✅ 无 UKEY 物理设备，完全无法登录
- ✅ UKEY 拔掉后可选配置会话立即失效
- ✅ 离职后管理员吊销，UKEY 即使未交还也无法登录

---

#### B-2. 数据库表静态脱敏导出

**功能目的**：提供独立的"导出工具"，让业务人员/审计员可以把生产库的指定表脱敏后导出为文件。

**详细功能点**：

| 编号 | 功能 | 说明 |
|---|---|---|
| B-2.1 | 导出任务 | 选源库 → 选表/字段 → 选规则 → 选格式 → 执行 |
| B-2.2 | 脱敏规则 | 复用 A-3/A-4 规则 |
| B-2.3 | 输出格式 | CSV、XLSX、JSON、SQL INSERT、DMP |
| B-2.4 | 大文件分片 | 单文件 > 100MB 自动分片 + 压缩 |
| B-2.5 | 加密落盘 | 输出文件**本身加密**（SM4） |
| B-2.6 | 下载授权 | 下载链接**有时效**（默认 1 小时）+ 一次性 Token |
| B-2.7 | 水印注入 | 导出文件**每行带隐藏水印**（含操作人、时间） |
| B-2.8 | 操作审计 | 完整记录：谁、导出什么、用什么规则、文件落到哪 |

**验收标准**：
- ✅ 导出文件无明文敏感数据
- ✅ 100 万行导出 ≤ 30 分钟（参考值）
- ✅ 文件离开本机后，水印可还原到操作人

---

#### B-3. 应用/数据库零改造透明加密

**功能目的**：业务系统**应用代码和数据库结构完全不改**，通过部署加密网关代理，自动对存储的敏感数据加密、对查询的敏感数据解密/脱敏。

**架构**：

```
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│ 业务应用  │───▶│ 加密网关  │───▶│  数据库   │    │  KMS/HSM │
│ (零改造)  │◀───│ (代理)    │◀───│ (零改造)  │    │          │
└──────────┘    └──────────┘    └──────────┘    └──────────┘
                    │
                    ├─ 协议解析（SQL 解析）
                    ├─ 字段识别（敏感字段）
                    ├─ 加解密引擎
                    ├─ 性能优化（连接池、缓存）
                    └─ 审计日志
```

**详细功能点**：

| 编号 | 功能 | 说明 |
|---|---|---|
| B-3.1 | 部署模式 | **反向代理模式**（推荐）：业务应用连接网关地址，网关转发到真实 DB |
| B-3.2 | 协议支持 | MySQL/PostgreSQL/Oracle/SQL Server 原生协议；**SQL 语法零修改** |
| B-3.3 | 加密粒度 | 表级、字段级两种 |
| B-3.4 | 加密算法 | SM4（默认）、AES-128/256；支持国密合规 |
| B-3.5 | 完整性校验 | 字段级 HMAC，**防篡改** |
| B-3.6 | 索引字段保护 | 加密后仍可建立索引（确定性加密） |
| B-3.7 | 密文检索 | 支持 LIKE 模糊查询（在密文上做检索） |
| B-3.8 | 透明加解密 | INSERT 自动加密、SELECT 自动解密；应用无感知 |
| B-3.9 | 动态脱敏 | 同一条数据，不同用户看到不同结果 |
| B-3.10 | 性能影响 | 加密后读写延迟增长 ≤ 15%（需基准测试） |

**关键场景示例**：

**场景 1：应用零改造**

```sql
-- 业务应用执行（不需要改）：
INSERT INTO user (id_card, mobile) VALUES ('110101199001011234', '13812345678');
SELECT id_card, mobile FROM user WHERE id = 1;

-- 网关内部自动完成：
-- ① 解析 SQL，识别 id_card/mobile 是敏感字段
-- ② 调 KMS 拿密钥
-- ③ INSERT 时加密成密文后落库
-- ④ SELECT 时读出密文，解密后返回（按用户权限脱敏）
```

**场景 2：密文检索**

```sql
-- 用户要查"身份证开头 110101 的人"
SELECT * FROM user WHERE id_card LIKE '110101%';
-- 网关识别后，转成密文前缀匹配，仍然走索引
```

**场景 3：完整性校验**

```sql
-- DBA 直接连 DB 改一条数据
UPDATE user SET id_card = '999999...' WHERE id = 1;
-- 应用再次读取时，网关发现 HMAC 不匹配，拒绝返回 + 告警
```

**验收标准**：
- ✅ 业务系统**源代码 0 行修改**
- ✅ 数据库结构**0 变更**（表结构、字段类型都不动）
- ✅ 加密后业务功能 100% 正常（CRUD、JOIN、聚合、索引全部可用）
- ✅ DBA 拿不到明文，绕过应用直接连库也看不到明文
- ✅ 100 万条加密 ≤ 30 分钟（参考值）

---

## 5. 非功能性需求

| 类别 | 要求 |
|---|---|
| **性能** | 100 万条数据：静态脱敏 ≤ 30min、在线洗数 ≤ 30min、反洗数 ≤ 30min、加密 ≤ 30min |
| **可用性** | 99.99% SLA，年停机 ≤ 52 分钟 |
| **并发** | 单网关支持 ≥ 5000 TPS（SELECT），≥ 1500 TPS（INSERT 含加密） |
| **数据库兼容** | MySQL 5.7/8.0、PostgreSQL 12+、Oracle 11g+、SQL Server 2016+、达梦、人大金仓、TiDB、OceanBase |
| **操作系统兼容** | Windows 7/10/11、CentOS 7+、Ubuntu 18+、麒麟、统信 UOS |
| **安全合规** | **国密局 SM2/SM3/SM4 算法**、等保三级、商用密码应用安全性评估（密评） |
| **审计** | 所有操作**不可篡改留痕**（含操作人、时间、对象、行为、结果） |
| **灾备** | 网关自身支持主备/集群；配置和密钥支持跨机房容灾 |
| **部署** | 支持物理机、虚拟机、容器（K8s）、信创平台 |
| **可维护性** | 关键组件提供 RESTful 管理 API；日志结构化输出；支持 Prometheus 监控指标导出 |
| **可扩展性** | 模块化设计，新增脱敏算法/数据源类型支持插件化扩展 |

---

## 6. 验收标准

### 6.1 功能验收

所有 §4 中的功能点必须 100% 通过测试用例验收。Bug 修复 SLA：

| 等级 | 含义 | 响应时间 |
|---|---|---|
| P0 | 系统不可用 | 4 小时 |
| P1 | 核心功能不可用 | 1 个工作日 |
| P2 | 一般功能问题 | 3 个工作日 |
| P3 | 优化建议 | 评估排期 |

### 6.2 安全验收

- 渗透测试无高危漏洞
- 代码审计无重大安全缺陷
- 国密合规性验证（SM 算法、HSM 集成）
- 审计日志完整性验证（任何操作都能查到）

### 6.3 业务验收

- 100 万条真实业务数据（脱敏副本）端到端跑通
- UKEY 拔掉、证书吊销、离职解绑等异常场景全部覆盖
- DBA 直连数据库看到的是密文（**核心安全验收**）

---

## 7. 交付物清单

| 序号 | 交付物 | 说明 |
|---|---|---|
| 1 | DSM 软件包 | 含 Console、网关、客户端、HSM 集成模块 |
| 2 | 数据库驱动包 | 支持的所有数据库 JDBC 驱动 |
| 3 | UKEY 驱动 | 主流 UKEY 厂商驱动（飞天诚信、握奇、格尔等） |
| 4 | 部署文档 | 详细安装、配置、HA 部署手册 |
| 5 | 运维手册 | 监控、告警、备份、应急响应流程 |
| 6 | API 文档 | 完整的 RESTful API 文档（OpenAPI 3.0） |
| 7 | 用户手册 | 管理员、运维、用户三类角色的操作手册 |
| 8 | 测试报告 | 功能测试、性能测试、安全测试报告 |
| 9 | 培训 | 管理员培训、运维培训、用户培训 |
| 10 | 售后 | 1 年免费维护期，7×24 故障响应 |

---

## 8. 实施路径建议

### 8.1 推荐方案：开源底座 + 二次开发

基于成熟开源项目进行定制开发，平衡成本与可控性：

| 选型 | 用途 | 工作量 |
|---|---|---|
| Apache ShardingSphere-Proxy | SQL 解析 + 改写（透明加密/脱敏底座） | 0.5 人月接入 |
| Bouncy Castle | 国密 SM2/SM3/SM4 算法库 | 直接使用，0 工作量 |
| ShardingSphere-Proxy / MyCAT | 数据库代理 | 1 人月集成 |
| Keycloak / Casdoor | UKEY / 证书认证 | 0.5 人月对接 |
| 自研 KMS 薄壳 | 密钥管理 + HSM 集成 | 1-1.5 人月 |
| Vue 3 + Naive UI | 管理后台 | 1 人月 |
| **合计** | | **3-5 人月** |

**优势**：
- SQL 协议解析不用从零写（ShardingSphere 已成熟）
- 国密算法 Bouncy Castle 原生支持
- 总成本低于自研一半
- 知识产权和后续迭代可控

### 8.2 备选方案 A：纯自研

适合有数据库中间件 + 国密开发经验的团队。总工作量 7-10 人月。

### 8.3 备选方案 B：采购商业产品

适合对合规兜底要求高、预算充足的场景。代表厂商：

| 厂商 | 优势 | 预算 |
|---|---|---|
| 数安商用密码 DSCC | 政务/金融强，密评 100% 通过 | 20-50 万 |
| 安华金和 | 数据库脱敏老牌 | 30-60 万 |
| 亿赛通 | DLP 强项 | 30-60 万 |
| 明朝万达 | 金融案例多 | 30-60 万 |

### 8.4 外包合作建议

#### 外包招标关键条款

| 条款 | 内容 |
|---|---|
| 知识产权 | 项目全部源代码、文档归甲方所有 |
| 源代码托管 | 提交至甲方指定 Git 仓库，每日 commit |
| 人员锁定 | 项目经理 + 技术负责人不得更换 |
| 里程碑付款 | 4 个里程碑，每个 20-30% |
| 验收标准 | 🔴+🟡 功能 100% 通过测试用例 |
| 维护期 | 验收后 1 年免费维护，7×24 故障响应 |
| 开源合规 | 乙方使用的所有第三方组件须符合许可协议 |
| 保密协议 | 违约赔偿 ≥ 项目金额 3 倍 |

#### POC 阶段建议

**POC 任务清单（2-3 周，少量预算 3-5 万）**：

1. 搭建 ShardingSphere-Proxy + Bouncy Castle，跑通字段加密 demo
2. 实现"一字段一密钥"的多字段 demo
3. 接入主流 UKEY 厂商（飞天诚信/握奇），走通登录全流程
4. 100 万条数据静态脱敏，记录耗时

**POC 交付物**：
- 源代码（甲方所有）
- 部署文档
- POC 测试报告
- POC 答辩 PPT

**POC 候选**（建议 2-3 家同时做）：
- 数安商用密码 DSCC（商业产品 + 集成服务）
- 有 ShardingSphere 经验的独立外包团队
- 一线城市的安全集成商

---

## 9. 风险与应对

| 风险 | 影响 | 应对措施 |
|---|---|---|
| 加密网关性能不达标 | 业务系统变慢 | 提前 POC 压测；选用成熟代理；分批上线灰度 |
| 国密算法集成出错 | 不满足密评 | POC 阶段重点验证 HSM 集成；选有经验外包 |
| 外包中途撂挑子 | 项目延期 | 源代码托管 + 人员锁定 + 违约金 |
| 数据迁移出问题 | 业务中断 | 灰度切换 + 一键回滚 + 完整备份 |
| 需求变更 | 范围蔓延 | 变更控制流程 + 影响评估 + 书面同意 |
| 密钥丢失 | 数据不可恢复 | 多副本备份 + 2 人授权 + 异地容灾 |

---

## 10. 附录

### 附录 A：技术选型理由

#### A.1 为什么选 ShardingSphere-Proxy？

- Apache 顶级项目，社区活跃
- 完整支持 MySQL/PostgreSQL 协议
- 内置字段加密（EncryptRule）+ 数据脱敏（ShardingSphere 内置 mask）
- 支持自定义 SPI 扩展，可深度定制
- 文档完善，中文社区活跃

#### A.2 为什么选 Bouncy Castle？

- 完整实现国密 SM2/SM3/SM4 算法
- 纯 Java 实现，无 native 依赖
- 商用友好（MIT 协议）
- 与 JDK 无缝集成

#### A.3 为什么选 Keycloak / Casdoor？

- 开源 IAM 标准方案
- 原生支持 X.509 证书认证
- 支持 OIDC/SAML，易于集成
- 社区活跃，文档齐全

#### A.4 为什么不直接用 MyCAT？

- 加密场景下需要深度定制，ShardingSphere 的 SPI 机制更灵活
- ShardingSphere 社区更活跃，迭代更快
- MyCAT 主要解决分库分表，加密非主战场

### 附录 B：参考标准

- GB/T 22239-2019《信息安全技术 网络安全等级保护基本要求》
- GB/T 39786-2021《信息安全技术 信息系统密码应用基本要求》
- GM/T 0054-2018《信息系统密码应用基本要求》
- 《中华人民共和国数据安全法》（2021-09-01 施行）
- 《中华人民共和国个人信息保护法》（2021-11-01 施行）
- 《中华人民共和国密码法》（2020-01-01 施行）

### 附录 C：变更记录

| 版本 | 日期 | 变更人 | 变更内容 |
|---|---|---|---|
| v1.0 | 2026-07-20 | 项目甲方 | 初稿，整合原始技术参数与多轮沟通结果 |

---

**文档结束**

> 评审意见请直接在此文档基础上批注。如需召开评审会，请提前 2 个工作日通知编写人。
